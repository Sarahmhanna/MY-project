server:
  port: 8085

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pfe
    username: root
    password: rootroot
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    ma.geo.local: INFO